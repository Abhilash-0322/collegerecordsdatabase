# 🏗️ System Architecture - College Management System

## 📊 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      CLIENT BROWSER                         │
│  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐ │
│  │Students│  │Faculty │  │Courses │  │Enroll. │  │Teaching│ │
│  │  Page  │  │  Page  │  │  Page  │  │  Page  │  │  Page  │ │
│  └────────┘  └────────┘  └────────┘  └────────┘  └────────┘ │
│       │          │            │           │           │     │
│       └──────────┴────────────┴───────────┴───────────┘     │
│                           │                                 │
│                    React Components                         │
│              (DataTable, Modal, Forms, etc.)                │
└───────────────────────────┬─────────────────────────────────┘
                            │
                     Axios HTTP Calls
                            │
┌───────────────────────────▼─────────────────────────────────┐
│                   NEXT.JS 14 SERVER                         │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              API ROUTES (Backend)                   │    │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐     │    │
│  │  │/api/       │  │/api/       │  │/api/       │     │    │
│  │  │ students   │  │ faculty    │  │ courses    │     │    │
│  │  └────────────┘  └────────────┘  └────────────┘     │    │
│  │  ┌────────────┐  ┌────────────┐                     │    │
│  │  │/api/       │  │/api/       │                     │    │
│  │  │ enrollment │  │ teaching   │                     │    │
│  │  └────────────┘  └────────────┘                     │    │
│  └─────────────────────────────────────────────────────┘    │
│                           │                                 │
│                    Prisma Client                            │
│                           │                                 │
└───────────────────────────┬─────────────────────────────────┘
                            │
                     Prisma ORM
                            │
┌───────────────────────────▼─────────────────────────────────┐
│                    MySQL DATABASE                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                   │
│  │ students │  │ faculty  │  │ courses  │                   │
│  └─────┬────┘  └────┬─────┘  └────┬─────┘                   │
│        │            │              │                        │
│  ┌─────▼────────────┼──────────────▼─────┐                  │
│  │    enrollments                         │                 │
│  │    (student_id, course_id, grade)      │                 │
│  └────────────────────────────────────────┘                 │
│        │                                                    │
│  ┌─────▼────────────┬──────────────────────┐                │
│  │    teachings                            │                │
│  │    (faculty_id, course_id, semester)    │                │
│  └─────────────────────────────────────────┘                │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Request Flow

### Example: Creating a Student

```
1. User fills form → Clicks "Create"
          ↓
2. Form submission → handleSubmit()
          ↓
3. Axios POST → /api/students
          ↓
4. Next.js API Route → POST handler
          ↓
5. Validation → Check required fields
          ↓
6. Prisma Client → prisma.student.create()
          ↓
7. MySQL Query → INSERT INTO students
          ↓
8. Response ← JSON with new student
          ↓
9. Frontend ← Update state + Toast
          ↓
10. UI Re-render ← Show new student in table
```

## 🎯 Component Architecture

```
app/layout.tsx (Root Layout)
├── Navbar
├── ToastProvider
└── Page Content
    │
    ├── app/students/page.tsx
    │   ├── useState (students, loading, formData)
    │   ├── useEffect (fetchStudents)
    │   ├── DataTable
    │   │   ├── Table Header
    │   │   ├── Table Rows
    │   │   └── Action Buttons (Edit, Delete)
    │   └── Modal
    │       └── Form
    │           ├── InputField (name)
    │           ├── InputField (dob)
    │           ├── InputField (department)
    │           ├── InputField (gender)
    │           ├── InputField (phone)
    │           ├── InputField (email)
    │           └── Buttons (Cancel, Submit)
    │
    ├── app/enrollment/page.tsx
    │   ├── useState (enrollments, students, courses)
    │   ├── useEffect (fetch all data)
    │   ├── DataTable (with student/course names)
    │   └── Modal
    │       └── Form
    │           ├── SelectField (student dropdown)
    │           ├── SelectField (course dropdown)
    │           ├── InputField (grade - optional)
    │           └── Buttons (Cancel, Submit)
    │
    └── (Similar structure for other pages)
```

## 🗄️ Database Schema

```
┌─────────────────────────────────────────────────────────────┐
│                      RELATIONAL MODEL                       │
└─────────────────────────────────────────────────────────────┘

Student                          Enrollment
┌──────────────┐                ┌──────────────┐
│ student_id   │◄───────────────│ enrollment_id│
│ name         │                │ student_id   │
│ dob          │                │ course_id    │
│ department   │                │ grade        │
│ gender       │                └──────────────┘
│ phone        │                        │
│ email (UK)   │                        │
└──────────────┘                        ▼
                                ┌──────────────┐
                                │ course_id    │
Faculty                         │ course_name  │
┌──────────────┐                │ department   │
│ faculty_id   │                │ credit       │
│ name         │                └──────────────┘
│ department   │                        ▲
│ designation  │                        │
│ phone        │                ┌──────────────┐
│ email (UK)   │────────────────► teaching_id  │
└──────────────┘                │ faculty_id   │
                                │ course_id    │
                                │ semester     │
                                └──────────────┘

Notation:
PK = Primary Key (student_id, faculty_id, etc.)
FK = Foreign Key (student_id in enrollment)
UK = Unique Key (email)
CASCADE = Cascade on delete
```

## 🔐 Data Flow & Security

```
┌─────────────────────────────────────────────────────────────┐
│                    VALIDATION LAYERS                        │
└─────────────────────────────────────────────────────────────┘

Layer 1: Frontend Validation
├── Required field checks
├── Email format validation
├── Number format validation
└── Date format validation
          ↓
Layer 2: API Route Validation
├── Request body validation
├── Type checking
├── Business logic validation
└── Authorization (if added)
          ↓
Layer 3: Prisma Validation
├── Schema validation
├── Type safety
├── Relation validation
└── Unique constraint checks
          ↓
Layer 4: Database Constraints
├── PRIMARY KEY
├── FOREIGN KEY
├── UNIQUE constraints
├── NOT NULL constraints
└── CASCADE rules
```

## 📦 Technology Stack Layers

```
┌─────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                       │
│  React 18 + Next.js 14 (App Router) + TypeScript            │
│  Tailwind CSS + React Hot Toast                             │
└────────────────────┬────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────┐
│                   APPLICATION LAYER                         │
│  Next.js API Routes + TypeScript                            │
│  Business Logic + Validation                                │
└────────────────────┬────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────┐
│                     DATA ACCESS LAYER                       │
│  Prisma ORM + TypeScript Types                              │
│  Query Builder + Migrations                                 │
└────────────────────┬────────────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────────────┐
│                   PERSISTENCE LAYER                         │
│  MySQL 8.0+ Database                                        │
│  Relational Tables + Constraints                            │
└─────────────────────────────────────────────────────────────┘
```

## 🚀 Development to Production Flow

```
Development Environment
┌─────────────────────────────────────────────────────────────┐
│  Local Machine                                              │
│  ├── npm run dev (Port 3000)                                │
│  ├── Hot Module Replacement                                 │
│  ├── Local MySQL Database                                   │
│  └── Prisma Studio (Port 5555)                              │
└─────────────────────────────────────────────────────────────┘
          │
          │ npm run build
          ▼
Production Build
┌─────────────────────────────────────────────────────────────┐
│  Optimized Static Files                                     │
│  ├── Server Components                                      │
│  ├── Client Components                                      │
│  ├── API Routes                                             │
│  └── Static Assets                                          │
└─────────────────────────────────────────────────────────────┘
          │
          │ Deploy
          ▼
Production Environment
┌─────────────────────────────────────────────────────────────┐
│  Vercel / Docker / VPS                                      │
│  ├── npm start (Production Server)                          │
│  ├── Environment Variables                                  │
│  ├── Remote MySQL Database                                  │
│  └── CDN for Static Assets                                  │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 State Management Pattern

```
Component Lifecycle:

1. Component Mount
   ├── useState initialization
   ├── useEffect runs
   └── fetchData() called
           ↓
2. Data Fetching
   ├── Set loading = true
   ├── Axios GET request
   ├── Receive response
   ├── Set data state
   └── Set loading = false
           ↓
3. Render
   ├── Show loading spinner (if loading)
   ├── Show empty state (if no data)
   └── Show DataTable (if data exists)
           ↓
4. User Action (e.g., Edit)
   ├── Open modal
   ├── Pre-fill form
   └── Wait for submission
           ↓
5. Form Submit
   ├── Prevent default
   ├── Validate inputs
   ├── Send API request (PUT/POST)
   ├── Handle response
   ├── Show toast notification
   ├── Close modal
   └── Re-fetch data
           ↓
6. Update UI
   └── Re-render with new data
```

## 🎨 UI Component Hierarchy

```
Root Layout (app/layout.tsx)
│
├── <html>
│   └── <body>
│       ├── Navbar
│       │   └── Navigation Links
│       │
│       ├── <main>
│       │   └── {children} (Page Content)
│       │       │
│       │       ├── Students Page
│       │       │   ├── Header + "Add" Button
│       │       │   ├── DataTable
│       │       │   │   ├── <table>
│       │       │   │   │   ├── <thead>
│       │       │   │   │   └── <tbody>
│       │       │   │   │       └── Rows with Edit/Delete
│       │       │   │   └── Loading/Empty States
│       │       │   └── Modal (conditionally rendered)
│       │       │       └── Form
│       │       │           ├── InputFields
│       │       │           └── Buttons
│       │       │
│       │       └── (Other pages follow similar pattern)
│       │
│       └── ToastProvider
│           └── Toast Notifications
│
└── Global Styles (Tailwind)
```

## 📊 Data Relationships

```
One-to-Many:
Student (1) ──── (N) Enrollment
Faculty (1) ──── (N) Teaching
Course (1) ──── (N) Enrollment
Course (1) ──── (N) Teaching

Many-to-Many (through junction tables):
Student ←─── Enrollment ───→ Course
Faculty ←─── Teaching ───→ Course

Cascade Rules:
DELETE Student → CASCADE delete Enrollments
DELETE Faculty → CASCADE delete Teachings
DELETE Course → CASCADE delete Enrollments + Teachings
```

---

## 🎯 Key Architectural Decisions

1. **Next.js App Router** - Modern React Server Components
2. **TypeScript** - Type safety throughout
3. **Prisma** - Type-safe database access
4. **Component Reusability** - DRY principles
5. **Client-Side State** - useState + useEffect pattern
6. **Toast Notifications** - Better UX feedback
7. **Modal Forms** - Non-blocking UI
8. **Cascade Deletes** - Data integrity
9. **Responsive Design** - Mobile-first approach
10. **Environment Variables** - Configuration management

---

**This architecture ensures scalability, maintainability, and production-readiness! 🚀**

*Last Updated: November 16, 2025*